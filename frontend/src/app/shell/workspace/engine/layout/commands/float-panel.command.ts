import type {
    PanelId,
    WorkspaceState,
} from "../../../model";

export function executeFloatPanelCommand(
    state: WorkspaceState,
    _panelId: PanelId,
): WorkspaceState {

    return state;

}