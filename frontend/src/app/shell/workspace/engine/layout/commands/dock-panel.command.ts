import type {
    PanelId,
    WorkspaceState,
} from "../../../model";

export function executeDockPanelCommand(
    state: WorkspaceState,
    _panelId: PanelId,
): WorkspaceState {

    return state;

}